using multiple nodes

adapter 
    if you need multiple io servers, you need to replace the default in-memory adapter 

radis streams adapter : 
    handle temporary diconnection to the redis Server and resume the stream without losing any packets 

node js cluster


node is single thread, so we need to scale the node program to run all the hardware resources 

    nodes : 
        primary 
            the defualt gateway receive the clients request and return data    
        workers 
            adapter : broadcast and dsitripute the connections 
            have multi computers 
                    have multi cores and threads 

the cluster manage the multi node copies, each node is running on its thread 
 

